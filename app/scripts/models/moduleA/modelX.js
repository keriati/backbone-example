/*global define*/

define([
    'underscore',
    'backbone'
], function (_, Backbone) {
    'use strict';

    var ModelaModel = Backbone.Model.extend({
        defaults: {
        }
    });

    return ModelaModel;
});
